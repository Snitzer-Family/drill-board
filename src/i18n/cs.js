// Czech. See en.js for the rules every dictionary follows — especially the
// carrier-noun rule: Czech declines, so a bare {placeholder} can never carry
// the case. Put it on a carrier noun beside the placeholder.

export default {
  /* ── Tune → Display: the language setting ── */
  "prefs.language": "Jazyk",
  "prefs.lang.auto": "Auto",
  "prefs.lang.hint.auto": "řídí se jazykem telefonu — nyní {lang}",
  "prefs.lang.hint.pinned": "nastaveno na {lang}, jazyk telefonu se ignoruje",
};
