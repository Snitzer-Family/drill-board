export const APP_VERSION: string;
export const DSL_VERSION: number;
export const RINK: { W: number; H: number };
export const VIEWS: Record<string, [number, number, number, number]>;
export const COLORS: readonly string[];
