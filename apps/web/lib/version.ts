// The website's own version, shown in the footer. Bump on a behavioural change
// to apps/web. The board has its own APP_VERSION in drill-core/src/constants.js;
// the two ship independently, so they must not share a number.
export const SITE_VERSION = "0.1";
